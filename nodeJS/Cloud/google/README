1. Promises in an http trigger
2. Write a firestore trigger which triggers when the database is updated.
3. 

Test firebse functions locally

1. compile into javascript
    a. cd into functions and run 
    npm run-script lint  // Tocheck
    npm run-script build // t compile the typescript
    firebase serve --only functions // To run the functions

    Can use curl to touch the url

    Remember for http clients we need to send a response at the end of the function.
    background triggers - return a promise 